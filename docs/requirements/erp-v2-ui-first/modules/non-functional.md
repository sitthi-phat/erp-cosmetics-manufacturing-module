# Non-Functional Requirements (NFR) — ESSENCE Hub System

slug: `erp-v2-ui-first` · per-module canonical · PO · 2026-07-29 (**+ Route/DN r11 2026-07-30 · Q1=A LOCKED · + Audit non-read+login r12 2026-07-30 · + Return `RET-…` numbering + Deferred-controls register 2026-07-31 · ★★★★★★ + CUMULATIVE-level RBAC r16 2026-07-31 · ★★★★★★★ + Platform/NFR decisions r17 2026-07-31 · ★★★★★★★★ + Notification READ-DRIVEN r18 2026-07-31 · ★★★★★★★★★ + Notification event-set update (+Follow-up/+Doc-Cancelled-Rejected/+Route-delivered; SP Low+Overstock DAILY; −Customer-Inactivity; −FG→Low real-time) r19 2026-07-31 · ★★★★★★★★★☆ + r19.1 correction: Route delivered → +owning Sale · Customer Follow-up = near-real-time ONLY 2026-07-31**) · **AUTHORITATIVE NFR SPEC**
ที่มา (locked): `brief.md` §5/§8 · ADR-000..009 · `scheduled-jobs.html` (J1–J7) · architecture · `entity-status-map.md` §1.6/§1.8/**§1.9/§1.10 (Route/DN r11)** · `stock-reservation.md` (Option A) · `deletion-policy.md` · scope D1–D18 · README §2/§3 · **`supply-planning.md` §5.1/§5c** · **`comment-convention.md`** · **`numbering-on-save.md` (G8)** · **`stock.md` §2b/§3b/§6** · **`bom.md` §5/§5c** · **`supplier.md` §10** · **`settings.md` §3/§4/§4b/§4d/§5/§6** · **`production.md` §5/§7 + `po.md` §5.2/§4b** · **`goods-receipt.md` §4/§9 + `qc.md` §4.1** · **★ `shipping.md` + `delivery-note.md` (Route/DN)** · **`traceability.md` §3/§4/§5b (trace-surface + non-read audit)** · **★★★★★★ `permission-matrix.md` §1a (cumulative RBAC)** · **★★★★★★★ r17 `platform.md` (noti/reset/self-disable) + gap-report `platform-gate2-gap-report.md` (H-B1/H-B2/H-B4/H-C1 · M-7)** · **★★★★★★★★★ r19/r19.1 `platform.md` §7 (noti event set) · `customer.md` §4.1 (follow-up)**

## สรุปภาษาไทย
รวม **ข้อกำหนดที่ไม่ใช่ฟังก์ชัน (NFR)** ทั้งหมดของ ESSENCE Hub ไว้ที่เดียว. ครอบ: **★★★★★★★ Performance (ปอนด์ 2026-07-31 — เลขใหม่): อ่าน AVG ≤ 200ms/MAX ≤ 1s · เขียน AVG ≤ 1s/MAX ≤ 3s · rate-limit ~30 concurrent/module** (แทนเลขเก่า <2s/50 concurrent), >200 PO/วัน + GR>50/วัน, **Auth/Session** (local+Google, 24h + เตะออก 06:00 + **★★★★★★★ เตือนล่วงหน้า 5 นาที + mid-action expiry redirect**, **★★★★★★★ password policy ≥8 ตัว lower+upper+digit + storage encrypt+hash + forget/reset password (single-use 3-วัน email) + account self-disable/Admin re-enable + no auto-lockout**, **★★★★★★ RBAC = CUMULATIVE per-module level (ลำดับชั้นสะสม R < C < U < D < A < Admin) — D14**), **Audit** (field-level, retention 1 ปี; ทุกกิจกรรม non-read ทุก module + login/logout), **Backup/Infra** (GCP Cloud Run + Cloud SQL MySQL; **★★★★★★★ Environments: Dev=local · Test(UAT)+Prod=GCP**), **Data/Format** (Asia/Bangkok, พ.ศ./UTC, gapless + number-on-save G8; THB, VAT ตาม invoice date; `RT-…`/`RET-…`; **★★★★★★★ i18n-ready ไทยอย่างเดียวเฟสนี้**), **Scheduled Jobs J1–J8**, **★★★★★★★ Notification = bell-only, event-driven + ★★★★★★★★ r18 read-driven (read แทน dismiss) + snackbar/shake + ไม่มี email · ★★★★★★★★★ r19 event set: +Follow-up/+Doc-Cancelled-Rejected/+Route-delivered, SP Low+Overstock DAILY (แทน FG→Low real-time), −Customer-Inactivity; คง PO→Production/QC/PR-auto/Invoice-Overdue/potential-delay · ★ r19.1: Route delivered → +owning Sale · Follow-up = near-real-time เท่านั้น**, **global search**, **responsive + ★★★★★★★ global submit-confirm contract + error-handling/empty-state**, **soft-delete + reference-guard**, **reliability/integrity**, **★★★★★★★ API & Application security (HTTPS/rate-limit/input-validation/CSRF)**, **★ Deferred-controls register (§15)**. ทุกข้อ derive จากค่าที่ล็อก — ไม่มีการเดา.

---

## 1. Performance & Capacity  *(★★★★★★★ r17 — เลขใหม่ ปอนด์ 2026-07-31 · แทน H-C1 เลขเก่า)*
| # | NFR | ค่า (locked ปอนด์ 2026-07-31) | เกณฑ์วัด (test oracle) | หมายเหตุ scope |
|---|---|---|---|---|
| **P1 Read** | เวลาตอบสนอง operation **อ่าน/ค้น/เปิดหน้า/list/detail/report** | **AVG ≤ 200 ms · MAX ≤ 1 s** | วัด server response time (TTFB ที่ API layer, ไม่รวม network/render) ต่อ read endpoint ภายใต้ normal load (≤ rate-limit P-RL) · AVG = ค่าเฉลี่ยต่อ endpoint ต่อชั่วโมง; MAX = ทุก read request ต้อง ≤ 1 s | ครอบทุกหน้าอ่านรวม quotation/so/supply-planning/production queue/**dashboard drill (aggregate read)**/qc/stock GR tab/**Route+DN list + trace/audit-log** |
| **P2 Write** | เวลาตอบสนอง operation **เขียน (create/save/status-change/void/cancel/approve/config)** | **AVG ≤ 1 s · MAX ≤ 3 s** | วัด server response time ของ write endpoint (รวม transaction commit + number-on-save atomic) ภายใต้ normal load · MAX = ทุก write ต้อง ≤ 3 s | ครอบทุกการบันทึกรวม G8 number-on-save (RT+DN หลายเลข/รอบ), stock ledger write, outbox event |
| **P-RL Rate-limit** | จำกัดคำขอพร้อมกัน **ต่อ module** | **~30 concurrent requests / module** | เกิน ~30 concurrent/module → throttle (เช่น 429/คิว) โดยระบบไม่ล่ม; **enforcement บน GCP (Cloud Run + cache/queue tier) = TL/DevOps ออกแบบ** — spec นี้ระบุเพียงเป้า NFR | **★ แทนเพดาน "50 concurrent users" เดิม (ปอนด์ 2026-07-31)** |
| P3 | ปริมาณงานต่อวัน | **> 200 PO/วัน · GR > 50/วัน** | นับ throughput รายวัน | **★ r10: GR>50/วัน ผ่าน QC-gate ก่อน credit** |
| P4 | Dashboard aggregate | = P1 Read (**AVG ≤ 200 ms · MAX ≤ 1 s**) | เป็น aggregate read; MAX ≤ 1 s | 7 แผนก/29 tile + drill |
| P5 | Client polling | dashboard 15s · noti bell 15s | **notification ปรากฏบน bell ≤ 15 s หลัง event** (ฝั่งเบราว์เซอร์) · **★★★★★★★★ r18: read auto-clears บน bell ตอน poll ถัดไป (≤ 15 s) — near-real-time ไม่จำเป็น** | polling interval |

> **★★★★★★★ (ปอนด์ 2026-07-31, ปิด H-C1) แทนเลขเก่า (P1 <2s hard 3s / P2 50 concurrent) ด้วยเป้าใหม่ที่วัดได้:** read AVG 200ms/MAX 1s · write AVG 1s/MAX 3s · rate-limit ~30 concurrent/module. number-on-save (G8) ต้อง atomic ต่อชนิด+ปี/เดือน กันเลขชนภายใต้ concurrency (รวม RT+DN หลายเลข/รอบ). capacity/enforcement planning = DevOps/TL (Stage 4).

## 2. Auth / Session / Access Control
| # | NFR | รายละเอียด (locked) |
|---|---|---|
| A1 | Login | **local (basic) + Google Login** (ADR-007) · **★ หน้า login เสนอทางเลือก basic vs Google** (platform.md §2/§4) · **★★★★ r12: login/logout = audit event (AU1)** · **★★★★★★★ r17: login มีลิงก์ "ลืมรหัสผ่าน" (A12)** |
| A2 | Session | **หมดอายุ 24 ชม.** + **บังคับ login ใหม่ทุกวัน 06:00** (J1) · **ไม่มีกะกลางคืน** · **★★★★★★★ r17: เตือนล่วงหน้า 5 นาที ก่อนตัด** (banner "ระบบจะออกจากระบบเวลา … กรุณาบันทึกงาน") · **mid-action expiry:** session หมดระหว่างทำงาน → **redirect ไปหน้า login**; งานที่ยังไม่กดบันทึก = **ไม่ถูกบันทึก (zero mutation / ไม่กินเลข G8)**, login ใหม่แล้วเริ่มรายการเดิมได้ · **★ 5-นาที lead-time = ค่าที่ PO เสนอ (non-blocking, ยืนยันที่ Gate)** |
| A3 | RBAC | **★★★★★★ r16: CUMULATIVE per-module level (ลำดับชั้นสะสม)** — role ได้รับ **ระดับเดียวต่อ module** ตามลำดับ **`R < C < U < D < A < Admin`**; ระดับนั้น**รวมทุก action ที่ต่ำกว่าอัตโนมัติ** (เช่น ระดับ U → ทำ R/C/U ได้; ปุ่ม (D) ต้อง ≥ D; (Ad) ต้อง Admin). **⚠️ Create (C) อยู่ต่ำกว่า Update (U)** — total order ไม่ใช่ bit อิสระ. **เดิมเขียนว่า "generic RUCDAA ต่อ module (R/U/C/D/A/Admin bit)" — r16 reconcile ให้เป็น total order สะสม (ไม่ใช่ 6 bit อิสระ), D14 คงเป็นแกน RBAC.** authoritative = `permission-matrix.md` §1a · `settings.md` §4. **★ effective level = ต่อ module เอา "ระดับสูงสุด (max)" จาก role ที่ Active เท่านั้น** |
| A4 | Guard | เมนู/ปุ่ม/URL/API ทุกจุด · **★ user ที่ role ถูก Disabled/Deleted → 403 ทุกจุด** · **★ r11: แก้สถานะ DN โดยตรง = ต้องระดับ ≥ Shipping.Approve (A)** · **★★★★★★ r16: gate = ผู้ใช้ต้องมีระดับ ≥ รหัส (min level) ที่ปุ่มต้องการ** · **★★★★★★★ r17: บัญชี self-disabled/soft-deleted → 403 ทุกจุด (A11); เรียก API ตรง = 403 เสมอ (§16)** |
| A5 | Scope ใหม่ | เพิ่ม module: **Quotation / SO / Supply Planning** · **★ r11: DN = Module เอกสารแยก (delivery-note)** |
| **★ A6** | **Password provisioning + first-login change** | **โหมด 2 แบบ:** must-change-on-first-login / permanent · กรอก 2 ครั้ง + toggle show/hide · edit-user write-only · **★★★★ r12: password set/reset + first-login change = audit event (ไม่เก็บค่า)** · **★★★★★★★ r17 abandonment:** ผู้ใช้โหมด must-change ปิด/ออกก่อนตั้งรหัสใหม่สำเร็จ → **flag must-change ยังคงอยู่**, ครั้งถัดไปที่ login ถูกบังคับตั้งใหม่อีก (เข้าใช้งานไม่ได้จนกว่าตั้งสำเร็จตาม A9) |
| **★ A7** | **Google account link** | Admin ผูก user ↔ Google (1:1) · ยกเลิกผูก → basic · **★ คนขับ (driver) ใน Route = system user** |
| **★ A8** | **Admin-only sensitive areas** | **VAT · ข้อมูลบริษัท · Audit-log = ระดับ Admin เท่านั้น** · **★★★★★★ r16: force-override / undelete-restore / สร้าง-แก้ role level-matrix / จัดการ user = ระดับ Admin เช่นกัน** · **★★★★★★★ r17: re-enable บัญชีที่ self-disabled = Admin (A11)** |
| **★★★★★★★ A9** | **Password policy (ปอนด์ 2026-07-31 — "มาตรฐาน ครบทุกชนิดอักษร แต่ไม่เข้มเกิน", ปิด M-7)** | **PO เสนอ (non-blocking): ยาว ≥ 8 ตัวอักษร + ต้องมี ตัวพิมพ์เล็ก + ตัวพิมพ์ใหญ่ + ตัวเลข (อักขระพิเศษ = ไม่บังคับ)** · บังคับทุกจุดที่ตั้งรหัส: Settings (ตั้ง/รีเซ็ต), first-login change, reset-password · ผิดเกณฑ์ → error ระบุเงื่อนไขที่ยังไม่ผ่าน · **ยืนยันกฎเป๊ะที่ Gate** |
| **★★★★★★★ A10** | **Password storage (security)** | **เก็บแบบ encrypt + hash** (ไม่เก็บ plaintext / ไม่ส่งคืนรหัสเดิม) · edit-user = write-only · อัลกอริทึม = TL ออกแบบ |
| **★★★★★★★ A11** | **Account self-disable + re-enable (ปอนด์ 2026-07-31, ปิด H-B2/H-4)** | ผู้ใช้ **ปิดบัญชีตนเอง** ได้ (confirm popup + **audit event**) → **session ที่ active ถูกตัดทันที**; request ถัดไป → forced login + **บล็อก** (login ไม่ผ่าน, ข้อความ "บัญชีถูกปิดใช้งาน กรุณาติดต่อผู้ดูแลระบบ"). **Re-enable = Admin เท่านั้น** (restore ผ่าน Settings, A8). **★ ไม่มี auto-lockout ที่ใดในระบบ** — เส้นทางปิดบัญชีมีเพียง self-disable + Admin-managed. **Edge only-Admin:** Admin คนเดียวในระบบพยายาม self-disable → **บล็อก + เตือน "ต้องมี Admin ในระบบอย่างน้อย 1 คน — มอบสิทธิ์ Admin ให้ผู้อื่นก่อน"** |
| **★★★★★★★ A12** | **Forget / Reset password (ปอนด์ 2026-07-31, ปิด H-B1)** | login มีลิงก์ **"ลืมรหัสผ่าน"** → ขอรีเซ็ต → ระบบส่ง **ลิงก์ single-use อายุ 3 วัน** ไปยัง **อีเมลบนเรคคอร์ดผู้ใช้** (settings.md §3) → หน้า reset ตั้งรหัสใหม่ 2 ครั้ง + show/hide (ตามเกณฑ์ A9). **Edge:** ลิงก์ >3 วัน → "ลิงก์หมดอายุ"; ลิงก์ single-use ถูกใช้แล้วกดซ้ำ → invalidated ("ลิงก์นี้ถูกใช้แล้ว"); อีเมลไม่ตรง active user → **ข้อความทั่วไป ไม่เปิดเผยว่ามี/ไม่มีบัญชี (no account enumeration)**; บัญชี self-disabled/soft-deleted ขอ reset → ข้อความทั่วไปเช่นกัน (ไม่ปลดบล็อก). **★ email delivery = เฉพาะ password reset เท่านั้น — เฟสนี้ไม่มี email notification อื่น (bell-only, §7)** |

## 3. Audit & Traceability (NFR ระดับระบบ)
| # | NFR | รายละเอียด (locked) |
|---|---|---|
| AU1 | **Non-read audit (field-level) — ทุกกิจกรรมที่ไม่ใช่การอ่าน ทุก module** | **★★★★ r12: audit ครอบ "ทุกกิจกรรมที่ไม่ใช่การอ่าน" ในทุก module + login/logout** — เก็บ เวลา/ผู้ทำ/module/entity/**action(event)**/field/จาก→เป็น/เหตุผล (traceability.md §3/§4). **การอ่าน/ดู/ค้น = ไม่ audit.** ครอบ: **create · update · delete/void/cancel · approve · status-change · config-change (รวม ★★★★★★ role level-per-module edit) · password-reset · role/user-change · stock-movement · comment-edit · ★ login/logout/first-login-change (login สำเร็จ + ล้มเหลว) · ★★★★★★★ r17 account self-disable + Admin re-enable · password-reset-request/complete**. รายละเอียดตัวอย่าง: **★ Quotation:** create / edit→version / Convert-to-PO→Confirmed / cancel · **★ BOM/Supplier:** create/edit/ราคา/inactivate/reactivate · price-matrix · **★ BOM planning-param save-back** · **★ Settings:** role/user/VAT/company/**role level edit** · **★ Production:** รับงาน/เริ่มผลิต/ส่ง QC/พร้อมส่ง/Hold/rework/`actual_produced_qty`/loss · **★ PO edit:** field-level old→new + raise ⚑ follow-up · **★ number-on-save (G8):** การออกเลขครั้งแรก (QT/SO/PO/PR/GR+Lot/**RT+DN**/INV/PRD/**RET**; Batch derived) = entity-create event · **★★ r10 GR/QC:** GR object lifecycle + `GR (+)` credit + FIFO retro-link ตอน QC "ผ่าน" · **★★★ r11 Route/DN:** **Route lifecycle** + **DN status change** (ทั้ง Route "เสร็จสิ้น" process และ **แก้ตรง สิทธิ์ ≥ A**) + **comment DN บังคับ** · **★ comment ทุก object ธุรกรรม** (CC3/CC6). **แหล่ง viewer = `settings.md` US-SET-05 (Admin only) — ค้น user id/username + ช่วงวัน + filter module.** |
| AU2 | Retention | **online 1 ปี** แล้ว **Super User manual purge/archive** · ประวัติ comment ลบไม่ได้ · **★ Audit-log viewer = ระดับ Admin (A8)** · **★ r12: login/logout อยู่ใน retention เดียวกัน (1 ปี)** |
| AU3 | Stock ledger + movement audit | ทุก movement = **append-only ledger + reason + source ref บังคับ** (D15) · **★ audit + trace ทุก movement:** เพิ่มวัตถุดิบใหม่ · loss (−) · adjust (+) · **`GR (+)` (r10: ตอน QC "ผ่าน")** · FG-in (+) · surplus (+) · **return (−) (source ผูก Lot+RM+Supplier+RET)** · **CONSUME (−)** (lot มี stock; หลาย lot = FIFO) — entity=RM/Lot หรือ FG/Batch. **RM/BOM/FG code = user-defined unique + create-only-lock** |
| AU4 | GMP chain | Lot→Batch→line→PO(หรือ SO)→ลูกค้า→DN/INV · QT = head-of-chain สาย OEM, FG per-Batch · **★★ r10: Lot ผูก GR object; เข้าสาย genealogy เมื่อ QC "ผ่าน"** · **★ r12: Lot ผูก Return (`RET-…`, ย้อน Supplier)** · **★ Supply Planning "สั่งผลิต" = SO produce-to-stock → PRD/Batch → FG-in** · **★ r11: DN ผูก Route (RT) ต้นทาง; ตัด FG (FIFO per-Batch) ตอน DN "ส่งสำเร็จ"** |
| AU5 | Archive | text file · Super User เท่านั้น · ยืนยันก่อน export |
| **★★★★ AU6** | **Trace surface (r12)** | **`trace.html` มี entity/topic SELECTOR (ครอบทุก object §3 traceability) + ค้น id/key ของ topic + ช่วงวัน + dropdown ชนิดวัน (date-type, G2) + worked sample per object (forward+backward)** — ดู `traceability.md` §3.1/§5b. viewer จำกัดตามสิทธิ์ Read module ต้นทาง; audit-log viewer รวม = Settings Admin-only. |

## 4. Backup / Infrastructure
| # | NFR | รายละเอียด (locked) |
|---|---|---|
| I1 | Runtime | **GCP Cloud Run** (Phase 3) · Phase 2 = local PC |
| I2 | Database | **Cloud SQL (MySQL)** (ADR-000) |
| I3 | Backup | **รายวัน** — Phase 2 cron dump · Phase 3 Cloud SQL backup (J7) |
| I4 | Storage abstraction | ไฟล์แนบผ่าน **storage abstraction (GCS-ready)** (ADR-006) |
| **★★★★★★★ I5** | **Environments (ปอนด์ 2026-07-31)** | **Dev = local PC** · **Test (UAT) + Prod = GCP** (DevOps-managed) · Test/Prod แยก config/secret/DB · deploy pipeline/secret management/DB migration = DevOps (Stage 4) |

## 5. Data / Format / Localization
| # | NFR | รายละเอียด (locked) |
|---|---|---|
| D-F1 | Timezone | **Asia/Bangkok** · แสดง **พ.ศ.** / เก็บ **UTC** |
| D-F2 | Gapless numbering **+ ★ number-on-save (G8)** | **ต่อปี/เดือน (RT/DN = ต่อวัน)** — PO/**QT-**/**SO-**/PR/GR/PRD/Batch/DN/INV/**RT (RT แทน SHP, Q1=A DECIDED — drop SHP)**/**★ RET (ใบคืน, ต่อปี/เดือน, 2026-07-31)** (ADR-008) · void/**ยกเลิก GR** ไม่ทำให้เลขหาย · **★ เลขออก "ตอนบันทึกสำเร็จ" ไม่โชว์ล่วงหน้าบนหน้า create** · บันทึก → ออกเลข atomic + **popup ยืนยัน** · **หลายเลขต่อการบันทึก (GR+Lot · ★ RT+DN) → popup แสดงครบ (NS7)** · แก้/เวอร์ชันใหม่/void = เลขเดิม · **PRD/Batch = ออกเลขตอน action** · **PR auto = ไม่มี popup** · **QC record + master code = นอกขอบเขต G8** · **★★★★★★★ r17: cancel ของ submit-confirm (§9.1) = ไม่กินเลข (เลขออกเฉพาะเมื่อยืนยันสำเร็จ, NS5)**. รายละเอียด `numbering-on-save.md` |
| D-F3 | Currency/VAT | **THB** · VAT ตาม **invoice date** · ตัวหนังสือไทย · **★ margin simulation ใน Supply Planning = ก่อน VAT** |
| D-F4 | สถานะ/UI | ป้ายสถานะ**ภาษาไทย**เสมอ · **dropdown search: RM/FG ชื่อ+รหัส, Lot dropdown (+FIFO) · BOM component · Supplier price-matrix · customer dropdown (G4) · Supply Planning FG · Return RM ในล็อต (G7)** · Settings role/user · Production queue · **★ r10: stock "Good Receipt (RM)" tab ค้น GR/Lot/Supplier/ชื่อ+รหัส RM + วันที่รับ + filter สถานะ · qc ตรวจรับ/ตรวจแบตช์ (sub-tab OEM/Own-Brand)** · **★★★ r11: Route list ค้นคนขับ/username/route-id + ช่วงวันชนิดวัน · DN list ค้น คนขับ/username/route-id/PO-SO/วันลูกค้าต้องการรับ + filter สถานะ DN (6)** · **★★★★ r12: trace entity/topic selector + ค้น id/key + ช่วงวันชนิดวัน · audit-log ค้น user id/username + ช่วงวัน + filter module** |
| D-F5 | เลขเอกสาร / รหัส master | `PO-…` · `QT-…` · `SO-…` · `INV-…` · `GR-…` · **`RT-{YYYYMMDD}-{NNNN}` (Route — RT แทน SHP, Q1=A DECIDED 2026-07-30 — drop SHP)** · `DN-{YYYYMMDD}-{NNNNN}` · **★ `RET-{YYYYMM}-{NNNNNN}` (ใบคืนวัตถุดิบ — gapless ต่อปี/เดือน, void-only; คนละ prefix กับ Route `RT-…`, 2026-07-31)** · Batch `B-{PO}-{line}-{run}` · **★ ทั้งหมดออกตอนบันทึก (G8/D-F2)** · **★ RM code = user-entered + unique + create-only-lock** · **★ FG/BOM code = user-entered (shared, 1:1) + unique + create-only-lock** |
| **★★★★★★★ D-F6** | **i18n-ready (ปอนด์ 2026-07-31)** | สร้าง **ภาษาไทยอย่างเดียวในเฟสนี้** (ไม่มี copy EN) แต่วางโครง (externalized strings / message keys / ไม่ hardcode สตริงใน UI) ให้ **เพิ่มภาษาอังกฤษภายหลังได้โดยไม่ต้องรื้อ UI** · **ไม่แปล EN ตอนนี้** |

## 6. Scheduled / Background Jobs (J1–J8) — locked + scope update
| Job | ความถี่ | ทำอะไร | แจ้งเตือน | idempotent |
|---|---|---|---|---|
| **J1** Session reset | ทุกวัน 06:00 | เลื่อน session_epoch | — | ✓ |
| **J2** Customer inactivity sweep | ทุกวัน | Active→Inactive (default 3 ด.) | **— (★★★★★★★★★ r19: noti Customer Inactivity ตัดออก — แทนด้วย Customer Follow-up #1; state sweep คงอยู่)** | ✓ |
| **J3** Invoice overdue sweep | ทุกวัน | **ส่งสำเร็จ (DN)** +เลยเครดิต+ยังไม่จ่าย → Overdue | ✓ Finance+Sale (noti หมวด 3 — **คงไว้**) | ✓ |
| **J4** Potential-delay | ทุกวัน/ชั่วโมง | PRD ใกล้ไม่ทันส่ง → badge | ✓ Sale+Stock (noti หมวด 2 — **คงไว้**) | ✓ |
| **J5** Notification outbox dispatcher | ต่อเนื่อง ~1s | ส่ง event ใน outbox (bell fan-out ตาม Read + snackbar/shake trigger) | ✓ | ✓ |
| **J6** Stock ledger integrity | ทุกคืน | เทียบ balance vs Σmovement | — (เตือน Admin) | ✓ |
| **J7** Database backup | ทุกวัน | Phase2 dump / Phase3 backup | — | ✓ |
| **★★★★★★★★★ J8** Supply Planning **Low + Overstock daily summary** (r19 — เดิม "low-stock digest") | **ทุกวัน ~06:00** | rollup: **จำนวน + รายการ FG (Active) ที่ Low** และ **จำนวน + รายการ FG ที่ Overstock** → **1 แจ้งเตือนสรุปใบเดียว** (ไม่ใช่ราย item) | ✓ Read Supply Planning (noti หมวด 2) — **deep-link หน้า SP เดิม filter Low+Overstock** | ✓ |

> **★★★★★★★ r17 หมายเหตุ deploy:** job ทั้งหมดต้องยิงได้จริงบน GCP Cloud Run — ถ้า min-instances = 0 ต้องใช้ **Cloud Scheduler → HTTP endpoint** (หรือ min-instances ≥ 1). การออกแบบ scheduler = DevOps/TL (gap H-D2).

**Not a job (on-read / event / client):** BOM cost badge · Supply-Planning cover/suggested/margin simulation (client) · **★ r11 Route "เสร็จสิ้น" (action, ไม่ auto-close)** · Inactive→Active · ปิด PR · dashboard/bell 15s · **gapless numbering = event ตอนบันทึกสำเร็จ (G8, atomic — รวม RT+DN หลายเลข/รอบ)** · stock movement = event + ledger write · **★★ r10: `GR (+)` credit = event ตอน QC "ผ่าน"** · **★★★ r11: Route/DN status change + DN status-edit(A) = event + audit** · BOM inactivate · save-back · production action + PO edit · role lifecycle · **★★★★ r12: login/logout/first-login-change = event + audit (ไม่ใช่ job)** · **★★★★★★★ r17: account self-disable/re-enable + password-reset-request/complete = event + audit** · **★★★★★★★★ r18: notification mark-read / mark-all-read = client/event (read-bit update ราย user; ไม่ยิง noti, ไม่ใช่ job)** = event + audit · **★★★★★★★★★ r19: Customer Follow-up noti (flag "ต้องติดตาม" ถึงกำหนด/ตั้ง) · Doc cancelled/rejected noti · Route delivered noti = event ตอนเกิด (near-real-time via outbox J5, ไม่ใช่ job)** · **★ r19.1: Customer Follow-up = near-real-time เท่านั้น (ไม่มี light daily due-check sweep); Route delivered noti fan-out = Read Shipping/Route + owning Sale**.

### 6.1 ★★★★★★★★★ Supply Planning DAILY Low + Overstock summary (r19 — ปอนด์ Gate-1 2026-07-31 · แทน real-time+digest ของ 2026-07-29)
- **Trigger:** rollup รายวัน **J8 ~06:00** — สรุปสุขภาพสต็อก FG (Active) ของวัน: **จำนวน + รายการ FG ที่ Low** และ **จำนวน + รายการ FG ที่ Overstock** เป็น **แจ้งเตือนสรุปใบเดียว** (ไม่ใช่ราย item, ไม่ใช่ real-time). **FG Inactive ไม่เข้าข่าย**.
- **★ r19: ตัด real-time FG→Low** (เดิมมี (a) real-time transition non-Low→Low) — Low แจ้งผ่านสรุปรายวันเท่านั้น. **FG→Low reservation/planning math คงเดิม** — เปลี่ยนเฉพาะ *การส่ง notification*. หน้า SP ยังมี on-screen Low bell/indicator ของตัวเอง (page feature, `supply-planning.md` §5.2) — แต่ในฐานะ *notification event* เหลือเฉพาะสรุปรายวัน.
- **Recipient:** มีสิทธิ์ Read Supply Planning (fan-out). **Deep-link → หน้า Supply Planning เดิม พร้อม filter query (Low + Overstock)** — reuse SP page (มี filter Low/OK/Overstock อยู่แล้ว `supply-planning.md` §4, ไม่สร้างหน้าใหม่). **(= notification หมวด 2, §7.)**

## 7. Notification (NFR) — ★★★★★★★ r17: Bell-only, event-driven · ★★★★★★★★ r18 READ-DRIVEN (read แทน dismiss) · ★★★★★★★★★ r19 event-set update · r19.1 correction (ปอนด์ 2026-07-31)
- **ช่องทางเดียว = กระดิ่ง (bell) ในเฟสนี้ — ไม่มี email notification** (email ใช้เฉพาะ reset password, A12). authoritative behavior + รายละเอียด per-event = `platform.md` §7.
- **Transactional outbox + read-bit ราย user** (J5/ADR-005): fan-out = ผู้มีสิทธิ์ **Read** ของ module ปลายทาง (ไม่ hardcode role). **★ r19.1 exception:** **Route ส่งสำเร็จ = Read Shipping/Route ∪ owning Sale** (เพิ่ม Sale เจ้าของงานให้ได้รับด้วย).
- **★★★★★★★★ read-driven (ปอนด์ r18 — read แทน dismiss):** badge = จำนวน **ยังไม่อ่าน (unread)** ราย user · **click-to-read** — คลิกแจ้งเตือน (ใน bell หรือหน้า "ดูทั้งหมด") = **navigate ไป deep-link + mark READ (ราย user; A อ่านไม่กระทบ B)** · รายการที่อ่านแล้ว **หลุดจาก bell ตอน refresh/poll ถัดไป** (**near-real-time ไม่จำเป็น — ยอมรับ poll delay (P5, ≤ 15s); ไม่มี websocket/batch ซับซ้อน**) · ปุ่ม **"อ่านแล้วทั้งหมด (mark all read)"** = mark ทุกรายการของ user เป็นอ่าน → bell เคลียร์ · หน้า **"ดูทั้งหมด"** (read-all history, 20/หน้า, จัดกลุ่ม 4 หมวด) แสดง **อ่าน/ยังไม่อ่าน** เก็บประวัติ. **ยกเลิก ✕ dismiss ราย item + "ปิดทั้งหมด" (dismiss-all) เดิมทั้งหมด — read เป็นกลไกเดียวที่เคลียร์ bell.**
- **★ snackbar + shake:** event ใหม่เข้ามา → แสดง **snackbar/toast ชั่วคราว (non-blocking, auto-dismiss)** + **bell สั่น (shake) animation**. snackbar = แจ้งข้อมูลชั่วคราว ≠ submit-confirm modal (§9.1).
- **deep-link** ต่อรายการ (คลิก = ไปเอกสารปลายทาง **+ mark read**) · badge cap **"9+"**.
- **★★★★★★★★★ Event set + 4-หมวด grouping (r19/r19.1 — definitive; รายละเอียด trigger→schedule→ผู้รับ→deep-link→read = `platform.md` §7):**
  1. **งานส่งต่อข้ามแผนก / เอกสาร (Workflow & Documents):** **PO Confirmed→Production** (คงไว้) · **QC ตรวจรับ RM/Batch ผ่าน/ไม่ผ่าน** (คงไว้) · **PR auto-created** (คงไว้) · **★ ใหม่ เอกสารถูกยกเลิก/ปฏิเสธ** (QT/PO/SO/RT/DN/Invoice + doc อื่นที่มี cancel/void/reject; **ไม่รวม user soft-delete**) · **★ ใหม่ Route ส่งสำเร็จ** (**★ r19.1: Read Shipping/Route + owning Sale**). **near-real-time** · ผู้รับ = Read ของ module ปลายทาง.
  2. **ความเสี่ยงสต็อก/การผลิต (Stock & Production risk):** **potential-delay (J4)** (คงไว้) · **★ เปลี่ยน — สรุป Supply Planning Low + Overstock รายวัน** (J8 ~06:00, count+list Low + count+list Overstock, **daily**, deep-link หน้า SP filter Low+Overstock) **แทน FG→Low real-time** (math คงเดิม). ผู้รับ = Read Production+Sale+Stock (delay) / Read Supply Planning (Low+Overstock).
  3. **การเงิน/เครดิต (Finance):** **Invoice Overdue (J3)** (คงไว้). ผู้รับ = Read Invoice (Finance+Sale).
  4. **วงจรลูกค้า (Customer lifecycle):** **★ ใหม่ — Customer Follow-up** (flag "ต้องติดตาม" ถึงกำหนด/ตั้ง, `customer.md` §4.1, **★ r19.1: near-real-time เท่านั้น — ตัดตัวเลือก light daily due-check ทิ้ง; รายการค้างใน bell อยู่แล้ว**) **แทน Customer Inactivity** (ตัดออก). ผู้รับ = Read Customer (Sale ที่ดูแล).
  - **★ r19 การเปลี่ยนแปลง:** เพิ่ม (Doc cancelled/rejected · Route delivered · Customer Follow-up) · เปลี่ยน (FG→Low real-time → Low+Overstock daily summary) · ตัด (Customer Inactivity). **คงไว้:** PO→Production · QC · PR auto · Invoice Overdue · potential-delay (J4).
  - **★ r19.1 correction (ปอนด์ 2026-07-31):** (1) **Route ส่งสำเร็จ** ผู้รับ = Read Shipping/Route **+ owning Sale** (เดิม Read Shipping/Route เท่านั้น). (2) **Customer Follow-up** schedule = **near-real-time เท่านั้น** (ตัด light daily due-check).
- **★ session/expiry ≠ notification:** การเตือน session 06:00/24h = banner (A2) ไม่ใช่ bell notification.
> **หมายเหตุ audit-not-notify:** การแก้ comment, stock movement, GR ส่งกลับ QC/ยกเลิก, production action + PO edit, BOM/supplier changes, Settings changes (รวม role level edit + account self-disable/re-enable), การออกเลขเอกสาร (G8), Route status change ระหว่างทาง (เตรียม/ออกไปส่ง), login/logout, **★★★★★★★★★ r19: Customer Active→Inactive (J2, noti ตัด) = state change แต่ไม่ยิง notification** = **audit event แต่ไม่ยิง notification** (ยกเว้นที่ระบุใน event set §7 ข้างบน) เว้นแต่ปอนด์ระบุเพิ่ม. **★★★★★★★★ r18: การ mark-read/mark-all-read = read-bit update ราย user (ไม่ยิง notification).** **★★★★★★★★★ r19: การลบเรคคอร์ดโดยผู้ใช้ (soft-delete) = ไม่ยิง notification (เฉพาะ cancel/void/reject เท่านั้นที่ยิงหมวด "เอกสารถูกยกเลิก/ปฏิเสธ").**

## 8. Global Search (NFR)
- ค้นข้าม entity จาก header · จำกัดตามสิทธิ์ Read · ≥ 2 ตัวอักษร · deep link.

## 9. Responsive / Usability (NFR — first-class)
- **Responsive ทุกหน้า (Must)** · minimize clicks · smart defaults · dropdown search ทุกจุด · ไม่มี enum ดิบ · **★ ทุกการเปลี่ยนสถานะในสายผลิต = confirm popup (production.md §7.7)** · **★ number-on-save = confirm popup แสดงเลข + summary หลังบันทึก (G8/NS3)** · **★ r10: ยกเลิก GR = confirm + เหตุผลบังคับ** · **★★★ r11: สร้าง Route → popup แสดงเลข RT + ทุก DN; "เสร็จสิ้น" = บังคับสรุปผลราย DN + comment; ลูกค้าเลื่อนส่ง = บังคับ next date** · **★★★★★★ r16: role editor = per-module level selector (radio/dropdown) + explainer สะสม + สรุป effective actions (settings.md §4/US-SET-01)**.
- **★★★★★★★ r17 Error handling ทั้งระบบ (ปอนด์):** หน้า error ที่เป็นมิตร (403/404/500 = ข้อความไทยเข้าใจง่าย + ทางออกกลับ) · **empty-state** ทุก list/ผลค้นหา ("ไม่พบข้อมูล …" + ปุ่มถัดไปถ้ามี) · **ไม่โชว์ stack trace / enum ดิบ / error ระบบต่อผู้ใช้**.

### 9.1 ★★★★★★★ Global submit-confirm contract (ปอนด์ 2026-07-31 — "ทุก submit ยืนยันก่อน ยกเว้นการค้นหา", ปิด H-B4)
- **ต้องมี confirm popup (MUST confirm):** ทุก action ที่ **เปลี่ยนข้อมูล** — สร้าง (create) · บันทึก (save/update) · เปลี่ยนสถานะ (status-change) · ลบ (delete) · void · ยกเลิก (cancel) · อนุมัติ (approve) · ตั้งค่า/config (รวม role level edit, VAT, company).
- **ต้อง NOT confirm (ห้ามมี popup):** ค้นหา (search) · อ่าน/เปิดดู (read/view) · กรอง (filter) · เปลี่ยนหน้า (pagination) — ยิงได้ทันที. **★★★★★★★★ r18: การอ่านแจ้งเตือน (คลิก = navigate + mark read) และ "อ่านแล้วทั้งหมด" = ไม่ใช่ mutation เชิงเอกสาร → ไม่ต้อง confirm popup (ยิงได้ทันที).**
- **โครง popup:** (1) **หัวเรื่อง** บอกการกระทำ · (2) **สรุปสิ่งที่จะเปลี่ยน** (เอกสาร/ค่า/สถานะ old→new; ถ้า number-on-save = แจ้งว่าจะออกเลขตอนยืนยัน) · (3) ปุ่ม **ยืนยัน** + **ยกเลิก** (label ชัดเจน).
- **★ Cancel = ไม่มีการเปลี่ยนแปลงใด ๆ:** **zero mutation** · **ไม่กินเลขเอกสาร** (ผูก numbering-on-save G8/NS5 — เลขออกเฉพาะเมื่อยืนยันสำเร็จ) · **ไม่มี audit event**.
- **★ แยกจาก snackbar (§7):** snackbar/toast = แจ้งผลชั่วคราว **หลัง** action สำเร็จ (transient/auto-dismiss/non-blocking) — **ไม่ใช่** submit-confirm; confirm popup = modal บล็อก **ก่อน** ทำ.
- number-on-save = confirm popup แสดงเลขหลังบันทึกสำเร็จ (G8/NS3) เป็นกรณีเฉพาะของสัญญานี้.

## 10. Soft-Delete + Reference-Guard (NFR baseline)
- **ไม่มี hard delete** · master = soft-delete · **BOM = inactivate** · **★ Role = Disable / Soft-delete** · **★★★★★★★ r17: บัญชีผู้ใช้ = soft-delete + self-disable (A11)** · เอกสารการค้า = void/cancel (**gapless — เลขที่ออกแล้วคงอยู่, G8/NS5**; **★ r10: ยกเลิก GR = void gapless เฉพาะก่อน credit**; **★ r11: ยกเลิก Route = void; DN คงเป็นประวัติ**; **★ ใบคืน `RET-…` = void-only, เลขคงอยู่**) · reference-guard · audit + comment บังคับ · undelete = ระดับ Admin. **★★★★★★★★★ r19: user soft-delete = ไม่ยิง notification (เฉพาะ workflow cancel/void/reject เท่านั้นที่ยิง noti หมวด "เอกสารถูกยกเลิก/ปฏิเสธ").**

## 11. Reliability / Integrity
| # | NFR | รายละเอียด |
|---|---|---|
| R1 | Append-only ledger | stock ledger เป็นความจริง — recompute ได้ (ADR-001) |
| R2 | Nightly integrity | J6 เทียบ 2 cache |
| R3 | Negative-stock policy | on_hand/Available ติดลบได้; **★★ r10: GR ชดเชย + FIFO retro-link เกิดตอน QC "ผ่าน"** |
| R4 | Reservation | ยืนยัน = จอง; เริ่มผลิต/dispatch = ตัดจริง (lot มี stock; หลาย lot = FIFO, Option A) · **★ r11: dispatch = ตอน DN "ส่งสำเร็จ"** |
| R5 | Transactional outbox | noti event (รวม **★★★★★★★★★ r19: Doc cancelled/rejected, Route delivered (r19.1: Read Shipping/Route + owning Sale), Customer Follow-up**, QC ตรวจรับ Lot ผ่าน, **★ r11 DN ส่งสำเร็จ**) เขียนใน tx เดียวกับ ledger/state · **★ number-on-save (G8) ออกเลข atomic (รวม RT+DN) ใน tx เดียวกับการบันทึก** |

## 12. Open question (NFR)
- **★ Q1 (r11) — RT vs SHP numbering: DECIDED (Q1=A, ปอนด์ 2026-07-30) — RT แทน SHP ทั้งหมด (drop SHP).** ดู `shipping.md` §12.
- **★★★★ r12 — login-failure audit (PO reasonable decision, ไม่ block):** audit บันทึก **ทั้ง login สำเร็จและล้มเหลว + logout**. ปอนด์ override ได้. **★★★★★★★ r17: no auto-lockout (A11) — ไม่มีนโยบาย lockout/rate-limit บัญชีจากการ login ล้มเหลว (rate-limit ระดับ API = §16 กัน abuse เท่านั้น).**
- **★★★★★★ r16 — cumulative-RBAC awkward-case (PO flag, NON-BLOCKING):** total order `R<C<U<D<A<Admin` ทำให้ **A รวม D เสมอ** และ **U รวม C เสมอ**. ปอนด์ยืนยัน ladder เข้มแล้ว → implement ตามนี้; ถ้าต้องการ SoD ละเอียดกว่านี้ → exception flag รอบถัดไป (DEF-2). ดู `permission-matrix.md` §1a/§4.
- **★★★★★★★ r17 — micro-decisions (PO เสนอค่า, NON-BLOCKING — สร้างสเปกไว้แล้วตามค่าเสนอ; ปอนด์ยืนยัน/ปรับที่ Gate):**
  1. **Session-expiry warning lead-time = 5 นาที** (A2) — เตือนก่อนตัด 5 นาที.
  2. **Password policy = ≥ 8 ตัว + lower + upper + digit (special ไม่บังคับ)** (A9).
- **★★★★★★★★★ r19/r19.1 — notification event set (ปอนด์ Gate-1 เคาะแล้ว · CLOSED):** เดิม 2 non-blocking flags → **ปอนด์เคาะแล้ว (r19.1 2026-07-31):** (1) **Customer Follow-up schedule = near-real-time เท่านั้น** — **ตัดตัวเลือก light daily due-check ทิ้ง** (รายการยังไม่อ่านคงค้างใน bell อยู่แล้ว จึงไม่ต้อง sweep รายวัน). (2) **Route delivered recipient = Read Shipping/Route + owning Sale** — Sale เจ้าของงานต้องได้รับด้วย. **ทั้งสอง CLOSED (ไม่ใช่ open question อีกต่อไป).**
- อื่น ๆ: **ไม่มี open question ค้าง**.

## 13. Cross-links
- Auth/session/RBAC → `settings.md` §3/§4/§4b (cumulative)/`platform.md` · **★★★★★★ RBAC cumulative model (authoritative) → `permission-matrix.md` §1a** · **★★★★★★★ r17 reset/self-disable/password-policy → `platform.md` §2/§4/§5 (US-PLT-06/07) · `settings.md` §3 (email field)/§6** · Audit → `traceability.md` (§3/§4/§5b) · ledger → `stock.md` §2b/§3b/§6 · **★ number-on-save (G8) → `numbering-on-save.md`** · **★ r10 GR object → `goods-receipt.md`/`qc.md`/entity-status-map §1.8** · production/PO edit → `production.md`/`po.md` · **★★★ r11 Route/DN → `shipping.md`/`delivery-note.md`/entity-status-map §1.9/§1.10/`po.md` §4b** · **★★★★ r12 non-read+login audit + trace surface → `settings.md` US-SET-05/§4d · `traceability.md` §3.1/§5b** · **★ Return `RET-…` → `return.md`/`numbering-on-save.md` §4** · BOM → `bom.md` · supplier → `supplier.md` · negative stock → entity-status-map §1.6/§1.8 · reservation → stock-reservation · soft-delete/role → `deletion-policy.md` · Low+Overstock daily summary → `supply-planning.md` · **★★★★★★★★★ r19/r19.1 noti event set → `platform.md` §7 · Follow-up (near-real-time เท่านั้น) → `customer.md` §4.1 · Doc cancelled/rejected → module เอกสาร · Route delivered (Read Shipping/Route + owning Sale) → `shipping.md`/`delivery-note.md`** · comment → `comment-convention.md`.

## 14. Module changelog
- **Consolidated + updated:** NFR ทั้งหมด → NFR module เดียว.
- **★ DECIDED (2026-07-29):** Supply Planning proactive Low alerting + J8. *(→ r19: real-time ตัด, J8 = Low+Overstock daily summary.)*
- **★ เพิ่ม (2026-07-29 — Quotation/comment/Stock/BOM/Supplier/Settings/Production/Supply Planning/number-on-save/QC+GR reviews):** (คงตามรอบก่อน — commit history)
- **★★★ เพิ่ม (2026-07-30 — Route/DN r11, ปอนด์ Module B/C):** **D-F2/D-F5** `SHP-…` → **`RT-…`** (Q1=A) · RT/DN gapless ต่อวัน; RT+DN popup NS7 · **AU1/AU3/AU4** Route/DN lifecycle + DN status-edit(A) + comment DN + ตัด FG ตอน DN "ส่งสำเร็จ" · **A4/A5/A7** DN status-edit = Shipping.A, DN = module แยก · **§6/§7/§9/§10/R4/R5** · **J3** overdue trigger = DN ส่งสำเร็จ · **D-F4** · **§12** Q1=A LOCKED.
- **★★★★ เพิ่ม (2026-07-30 — Audit non-read+login + Trace-surface r12, ปอนด์):** **AU1** rewrite = audit ครอบ "ทุกกิจกรรมที่ไม่ใช่การอ่าน" + login/logout/first-login-change · **AU2** retention 1 ปี ครอบ login · **AU4** Lot↔Return · **AU6 ใหม่** = trace surface · **A1/A6** login + password event = audit · **D-F4** · **§6/§7** login/logout = event ไม่ยิง noti · **§12** login-failure = audit.
- **★★ เพิ่ม (2026-07-31 — reconciliation M1+m4, ปอนด์):** **D-F2/D-F5** เพิ่มเลขใบคืน **`RET-{YYYYMM}-{NNNNNN}`** (แยกจาก Route `RT-…`) · **AU1/AU3/AU4/§10/§13** return ledger source RT→RET · **§15 ใหม่ = Deferred-controls register**.
- **★★★★★★ เพิ่ม (2026-07-31 — CUMULATIVE-level RBAC r16, ปอนด์):** **A3 rewrite** = RBAC generic RUCDAA+Admin bit → **cumulative per-module level (total order `R < C < U < D < A < Admin`)**: role ได้ระดับเดียวต่อ module, ระดับสูงรวม action ที่ต่ำกว่า (C < U), effective level = max ของ role Active · **A4** gate = ต้องระดับ ≥ min level ที่ปุ่มต้องการ · **A8** เพิ่ม force-override/undelete/role-matrix/จัดการ user = Admin · **AU1** config-change รวม role level edit · **§9** role editor = per-module level selector (usability) · **§12** flag awkward-case (A⊇D, U⊇C) non-blocking. authoritative = `permission-matrix.md` §1a · `settings.md` §4.
- **★★★★★★★ เพิ่ม (2026-07-31 — Platform/NFR decisions r17, ปอนด์; ปิด gap H-B1/H-B2/H-B4/H-C1 + M-7 + micro-decisions):**
  - **§1 Performance rewrite (ปิด H-C1):** แทนเลขเก่า (<2s/50 concurrent) ด้วย **P1 read AVG 200ms/MAX 1s · P2 write AVG 1s/MAX 3s · P-RL rate-limit ~30 concurrent/module** + test oracle ทุกแถว.
  - **§2 Auth:** **A2** warning lead-time **5 นาที** + mid-action expiry redirect · **A6** first-login abandonment · **A9 ใหม่** password policy (≥8 lower+upper+digit) · **A10 ใหม่** storage encrypt+hash · **A11 ใหม่** account self-disable + Admin re-enable + no auto-lockout · **A12 ใหม่** forget/reset password (single-use 3-วัน email, no-enumeration).
  - **§3 AU1:** เพิ่ม self-disable/re-enable + password-reset-request/complete = audit.
  - **§4 I5 ใหม่:** Environments Dev/Test(UAT)/Prod.
  - **§5 D-F6 ใหม่:** i18n-ready (ไทยอย่างเดียวเฟสนี้) · **D-F2** cancel ไม่กินเลข.
  - **§6 J-table:** ผูก J2/J3/J4/J8 กับ noti type; หมายเหตุ Cloud Scheduler (H-D2). *(→ r19: J2 noti ตัด, J8 = Low+Overstock daily.)*
  - **§7 Notification rewrite (ปิด H-B3 ฝั่ง NFR):** bell-only ไม่มี email · dismiss≠read · snackbar+shake · **4-type taxonomy**. *(→ dismiss≠read superseded by r18 read-driven; event set updated by r19.)*
  - **§9 + §9.1 ใหม่:** error handling/empty-state · **global submit-confirm contract** (ปิด H-B4; cancel = zero mutation/ไม่กินเลข/ไม่ audit).
  - **§16 ใหม่:** API & Application security (HTTPS/rate-limit/input-validation/CSRF).
  - **§12** เพิ่ม 2 micro-decisions (lead-time 5 นาที · password rule) non-blocking + no-lockout.
  - authoritative behavior = `platform.md` (noti/reset/self-disable) · `settings.md` §3 (email field).
- **★★★★★★★★ เพิ่ม (2026-07-31 — Notification READ-DRIVEN reversal r18, ปอนด์ Gate-1):** **§7 rewrite** จาก dismiss≠read → **read-driven**: outbox **read-bit ราย user** (แทน dismiss-bit) · **badge = unread** · **click = navigate deep-link + mark READ** (ราย user; A อ่านไม่กระทบ B) · read auto-clears จาก bell ตอน refresh/poll ถัดไป (P5 ≤ 15s; **ไม่ต้อง real-time, ไม่มี websocket/batch**) · **"อ่านแล้วทั้งหมด (mark all read)"** · **ยกเลิก ✕ dismiss ราย item + "ปิดทั้งหมด" (dismiss-all)** · "ดูทั้งหมด" = อ่าน/ยังไม่อ่าน + 4-หมวด group + 20/หน้า + deep-link. **§9.1** mark-read/mark-all-read = ไม่ต้อง confirm · **§6** mark-read = event ไม่ยิง noti · **§1 P5** read auto-clear บน poll. §7 + summary + §1/§6/§9.1 + §14. authoritative behavior = `platform.md` §7. **มี HTML view (`non-functional.html` render จาก .md) + ลิงก์ใน modules/index — no view ใหม่.**
- **★★★★★★★★★ เพิ่ม (2026-07-31 — Notification EVENT-SET update r19, ปอนด์ Gate-1):** ปรับ **event set** (ไม่แตะ r18 read-driven mechanics): **เพิ่ม** — (a) **Customer Follow-up** (flag "ต้องติดตาม" ถึงกำหนด/ตั้ง, near-real-time, Read Customer, deep-link customer detail) · (b) **เอกสารถูกยกเลิก/ปฏิเสธ** (QT/PO/SO/RT/DN/Invoice + doc อื่นที่มี cancel/void/reject; **ไม่รวม user soft-delete**, near-real-time, Read module เอกสาร, deep-link เอกสาร) · (c) **Route ส่งสำเร็จ** (near-real-time, Read Shipping/Route, deep-link Route/DN). **เปลี่ยน** — FG→Low **ตัด real-time** → **สรุป Supply Planning Low+Overstock รายวัน** (J8 ~06:00: count+list Low + count+list Overstock, Read Supply Planning, deep-link หน้า SP เดิม filter Low+Overstock; **reservation/planning math คงเดิม**). **ตัด** — **Customer Inactivity noti** (J2) → แทนด้วย Follow-up. **คงไว้ (ปอนด์ยืนยัน)** — **PO→Production · QC ผ่าน/ไม่ผ่าน · PR auto-created · Invoice Overdue (J3) · potential-delay (J4)**. UI คงจัดกลุ่ม **4 หมวด**. **§6 J2/J8** reconciled · **§6.1 rewrite** (daily Low+Overstock) · **§7 event set rewrite** · **§10** user soft-delete ไม่ยิง noti · **R5** outbox event. authoritative behavior = `platform.md` §7 · `supply-planning.md` §5.1 · `customer.md` §4.1. **ใช้ view เดิม (`non-functional.html` render จาก .md) + index card refreshed — no view ใหม่.**
- **★★★★★★★★★☆ เพิ่ม (2026-07-31 — Notification r19.1 correction, ปอนด์ Gate-1):** สอง correction บน event set (spec-only, ไม่แตะ mockup): (1) **Route ส่งสำเร็จ** ผู้รับ = **Read Shipping/Route + owning Sale** (เดิม Read Shipping/Route เท่านั้น) — Sale เจ้าของงานต้องได้รับด้วย. (2) **Customer Follow-up** schedule = **near-real-time เท่านั้น** — ตัดตัวเลือก "light daily due-check" (รายการยังไม่อ่านค้างใน bell อยู่แล้ว). แก้ header/summary + §6 not-a-job + §7 (#1 Route / #4 Follow-up + outbox exception) + §12 (2 flags CLOSED) + §13 + R5. authoritative behavior = `platform.md` §7. **ใช้ view เดิม (`non-functional.html` render จาก .md) — no view ใหม่.**
- **ไม่มีตัวเลขประดิษฐ์ (ทุกค่าเป็นค่าที่ปอนด์ล็อกหรือ PO เสนอ non-blocking ที่ทำเครื่องหมายไว้).**

## 15. ★ Deferred-controls register (relaxed-this-phase → re-tighten later) — NEW 2026-07-31
> รายการ **การควบคุมทางธุรกิจที่จงใจผ่อนในเฟสนี้** (มี note ในสเปกอยู่แล้ว แต่รวมไว้ที่เดียวเพื่อให้ปอนด์เห็นชัดทุก Gate และ QA เขียน AC ตามเจตนา — ไม่ใช่ bug/oversight). ปอนด์ยืนยัน/override ได้ที่ Gate.
| # | Deferred control | สถานะเฟสนี้ (relaxed) | เจตนาเมื่อ re-tighten | อ้างอิง |
|---|---|---|---|---|
| **DEF-1** | **Invoice Confirmed-gate** — สร้างใบแจ้งหนี้ได้กับ PO/SO **ทุกสถานะ** (แม้ยังไม่ Confirmed) | **ผ่อน:** ไม่ล็อกสถานะตอนสร้างใบ (create-no-status-lock) | ภายหลังจะกลับมาบังคับให้สร้าง Invoice ได้เฉพาะเมื่อ PO/SO ถึงสถานะที่กำหนด | `invoice.md` §7/§10 · README §2 · permission-matrix §3.1 (r13) |
| **DEF-2** | **RBAC separation-of-duties (SoD)** — total order สะสม `R<C<U<D<A<Admin` ทำให้ **A รวม D** และ **U รวม C** (ไม่แยก approve-without-delete / update-without-create) | **ผ่อน:** ใช้ ladder เข้มตามที่ปอนด์สั่ง (r16) — 1 ระดับ/module | ถ้าธุรกิจต้องการ SoD ละเอียด → เพิ่ม exception flag/action-level override นอก total order | `permission-matrix.md` §1a/§4 · `settings.md` §4 · non-functional §2 A3/§12 |
> วิธีดูแล: ทุกครั้งที่มี "การควบคุมที่จงใจผ่อน" ใหม่ ให้เพิ่มแถวที่นี่ + ชี้ไป module ต้นทาง. เมื่อ re-tighten แล้วให้ mark เป็น "CLOSED (วันที่)" ไม่ลบแถว.

## 16. ★★★★★★★ API & Application Security (NFR — ปอนด์ 2026-07-31, r17)
- **HTTPS** ทุก endpoint (Test/Prod บน GCP).
- **Rate-limit** (P-RL ~30 concurrent/module, §1) กัน abuse.
- **Input validation** ทุก request (server-side) — กัน injection/malformed payload.
- **CSRF protection** สำหรับทุก state-changing request.
- **RBAC ที่ API layer:** เรียก API ตรงข้าม guard/สิทธิ์ → **403 เสมอ** (สอดคล้อง A4) · บัญชี self-disabled/soft-deleted → 403 (A11).
- **เจตนา:** "un-hackable via API". **รายละเอียดการบังคับใช้บน GCP (Cloud Run + cache/queue tier, Redis boundary ฯลฯ) = TL/DevOps (gap H-A4).**
